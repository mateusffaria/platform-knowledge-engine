{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    { "type": "timeseries", "title": "Evidence Precision and Recall at K", "targets": [
      { "expr": "pke_eval_quality_evidence_precision_at_k_sum / pke_eval_quality_evidence_precision_at_k_count", "legendFormat": "precision@K" },
      { "expr": "pke_eval_quality_evidence_recall_at_k_sum / pke_eval_quality_evidence_recall_at_k_count", "legendFormat": "recall@K" }
    ], "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 } },
    { "type": "timeseries", "title": "Coverage and Missing-Evidence Accuracy", "targets": [
      { "expr": "pke_eval_quality_requirement_coverage_accuracy_sum / pke_eval_quality_requirement_coverage_accuracy_count", "legendFormat": "coverage" },
      { "expr": "pke_eval_quality_missing_evidence_accuracy_sum / pke_eval_quality_missing_evidence_accuracy_count", "legendFormat": "missing" }
    ], "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 } },
    { "type": "timeseries", "title": "Unsupported Selection and Provenance", "targets": [
      { "expr": "pke_eval_quality_unsupported_selection_rate_sum / pke_eval_quality_unsupported_selection_rate_count", "legendFormat": "unsupported" },
      { "expr": "pke_eval_quality_provenance_completeness_sum / pke_eval_quality_provenance_completeness_count", "legendFormat": "provenance" }
    ], "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 } },
    { "type": "timeseries", "title": "Reasoning Latency (Performance)", "targets": [
      { "expr": "pke_eval_performance_reasoning_latency_milliseconds_sum / pke_eval_performance_reasoning_latency_milliseconds_count", "legendFormat": "latency ms" }
    ], "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 } },
    { "type": "timeseries", "title": "Reasoning Tokens (Performance)", "targets": [
      { "expr": "pke_eval_performance_prompt_tokens_sum", "legendFormat": "prompt" },
      { "expr": "pke_eval_performance_completion_tokens_sum", "legendFormat": "completion" }
    ], "gridPos": { "h": 8, "w": 24, "x": 0, "y": 16 } }
  ],
  "schemaVersion": 39,
  "tags": ["pke", "evaluation", "quality"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "browser",
  "title": "PKE Evaluation Quality",
  "uid": "pke-evaluation-quality",
  "version": 1
}
